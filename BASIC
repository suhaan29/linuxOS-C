#include<stdio.h>
int main(int argc, char *argv[])
{
  int i;
  for(int i = 0; i <argc; i++)
   {
     if(argc > 1) {
     printf("ARG[%d] = %s", i ,argv[i]);
   }
   else
   {
   printf("enter more stuff");
   }
   }
 }  
